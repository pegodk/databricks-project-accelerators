-- Metric view setup has moved to notebooks/setup_metric_views.py
-- The setup job runs that notebook directly so catalog and schema
-- can be overridden at deploy time via: databricks bundle deploy --var catalog=<name>
