{% set slug = project_slug | replace('-', '_') %}
resources:
  genie_spaces:
    {{ slug }}_genie:
      title: "dpa-{{ project_slug }}"
      warehouse_id: ${var.warehouse_id}
      file_path: resources/genie_spaces/tpch_genie.geniespace.json
