{
  "display_name": "{{ cfg.genie_space_name }}",
  "description": "Ask natural-language questions about TPCH sales data. Powered by the v_tpch metric view in {{ cfg.catalog }}.{{ cfg.schema }}.",
  "instructions": "You are a sales analytics assistant for a global trading company. The data comes from the TPC-H benchmark dataset covering orders, customers, and geography (post-1995). All monetary values are in USD. The metric view v_tpch exposes dimensions (order_date, order_month, order_year, order_status [Open/Processing/Fulfilled], order_priority, customer_name, market_segment, customer_nation) and measures (order_count, total_revenue, unique_customers, avg_order_value, revenue_per_customer, open_order_revenue, fulfilled_order_revenue, t7d_customers). When asked about revenue without qualification, use total_revenue. For trend questions group by order_month. For geographic questions group by customer_nation. Always round monetary amounts to 2 decimal places.",
  "tables": [
    {
      "catalog": "{{ cfg.catalog }}",
      "schema": "{{ cfg.schema }}",
      "name": "v_tpch"
    }
  ],
  "curated_questions": [
    "What is the total revenue?",
    "How has monthly revenue trended over time?",
    "Which market segment generates the most revenue?",
    "What are the top 10 customers by total revenue?",
    "Which country has the highest order count?",
    "What is the average order value by market segment?",
    "How many unique customers have placed orders?",
    "What is the revenue split between open, processing, and fulfilled orders?",
    "What percentage of revenue comes from high-priority orders?",
    "Which country has the highest revenue per customer?"
  ]
}
