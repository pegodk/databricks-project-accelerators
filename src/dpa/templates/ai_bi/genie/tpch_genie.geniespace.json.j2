{
  "display_name": "{{ cfg.genie_space_name }}",
  "description": "Ask natural-language questions about TPCH sales data. Powered by metric views in {{ cfg.catalog }}.{{ cfg.schema }}.",
  "instructions": "You are a sales analytics assistant for a global trading company. The data comes from the TPC-H benchmark dataset and covers orders, customers, suppliers, line items, and geography. All monetary values are in USD. Use the metric views — v_kpis, v_revenue_by_month, v_revenue_by_segment, v_revenue_by_region, v_top_customers, v_shipmode_summary, and v_order_priority_summary — to answer questions. When asked about revenue, prefer total_revenue unless the question specifies gross or net. For trend questions, use v_revenue_by_month ordered by order_month. Customer questions should use v_top_customers. Always round monetary amounts to 2 decimal places in your responses.",
  "tables": [
    {
      "catalog": "{{ cfg.catalog }}",
      "schema": "{{ cfg.schema }}",
      "name": "v_kpis"
    },
    {
      "catalog": "{{ cfg.catalog }}",
      "schema": "{{ cfg.schema }}",
      "name": "v_revenue_by_month"
    },
    {
      "catalog": "{{ cfg.catalog }}",
      "schema": "{{ cfg.schema }}",
      "name": "v_revenue_by_segment"
    },
    {
      "catalog": "{{ cfg.catalog }}",
      "schema": "{{ cfg.schema }}",
      "name": "v_revenue_by_region"
    },
    {
      "catalog": "{{ cfg.catalog }}",
      "schema": "{{ cfg.schema }}",
      "name": "v_top_customers"
    },
    {
      "catalog": "{{ cfg.catalog }}",
      "schema": "{{ cfg.schema }}",
      "name": "v_shipmode_summary"
    },
    {
      "catalog": "{{ cfg.catalog }}",
      "schema": "{{ cfg.schema }}",
      "name": "v_order_priority_summary"
    }
  ],
  "curated_questions": [
    "What is the total revenue across all orders?",
    "How has monthly revenue trended over time?",
    "Which market segment generates the most revenue?",
    "What are the top 10 customers by lifetime revenue?",
    "Which region has the highest order count?",
    "What is the average order value by market segment?",
    "Which shipping mode has the highest average discount?",
    "How many unique customers have placed orders?",
    "What percentage of revenue comes from URGENT priority orders?",
    "Which nation has the highest revenue in the EUROPE region?"
  ]
}
